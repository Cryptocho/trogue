-- Game Configuration
-- Global constants shared across all systems

return {
    -- Rendering
    TILE_SIZE = 16,
    SCALE = 2,

    -- Map
    TILES_PER_ROW = 8,

    -- Animation
    MOVE_DURATION = 0.12,  -- seconds for a single tile move
}
