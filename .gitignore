# 构建产物
build/
build-release/

# 运行期生成的截图
screenshot_*.png

# 测试临时 fixture（写 assets/ 后由用例自行删除；进程被中断时会残留于此）
assets/__tmp_*
assets/tilesets/__tmp_*

# 原始项目只读参考副本（自带 .git，勿并入本仓库）
trogue-orign/

# Godot 引擎源码参考副本（只读查证用，不入仓库）
reference/

# Godot 本地视觉素材（美术源文件与导入元数据，不入仓库）
editor/assets/

compile_commands.json
build-asan/

# Python
__pycache__/
*.pyc
